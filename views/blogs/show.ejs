<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= blog.name %></title>
  </head>
  <body>
    <h1><%= blog.name %></h1>
   <% if (blog.isReadyToCreate) { %>
    <p>This blog is ready to create!</p>
  <% } else { %>
    <p>This blog is not ready to create!</p>
  <% } %>
  <form action="/blogs/<%=blog._id%>?_method=DELETE" method="POST">
    <button type="submit">Delete <%= blog.name %></button>
  </form>
  <a href="/blogs/<%= blog._id %>/edit">Edit <%= blog.name %></a>
  <a href="/blogs/">Back to Blogs</a>
  </body>
</html>
