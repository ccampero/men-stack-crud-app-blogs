
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit <%= blog.name %></title>
  </head>
  <body>
    <!-- update the header -->
    <h1>Edit <%= blog.name %></h1>
    <form action="/blogs/<%=blog._id%>?_method=PUT" method="POST">
      <label for="name">Name:</label>
      <input type="text" name="name" id="name" value="<%= blog.name %>"/>
  
      <label for="isReadyToCreate">Ready to Create?</label>
      <input type="checkbox" name="isReadyToCreate" id="isReadyToCreate" <% if (blog.isReadyToCreate) { %>checked<% } %> />
      <!-- update the button text -->
      <button type="submit">Update Blog</button>
    </form>
    <a href="/blogs">Back to Blogs</a>
  </body>
  